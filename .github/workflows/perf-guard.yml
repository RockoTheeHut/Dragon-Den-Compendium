name: Perf Guard

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  perf-guard:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Prepare database
        run: |
          python manage.py migrate --noinput

      - name: Run perf guard
        run: |
          # Wall-clock thresholds are unreliable on shared runners; gate on
          # query counts and payload sizes only.
          python scripts/perf_guard.py --skip-timing
